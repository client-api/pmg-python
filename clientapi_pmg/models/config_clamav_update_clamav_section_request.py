# coding: utf-8

"""
    Proxmox Mail Gateway API

    Generated from apidoc.js. NOT an official Proxmox specification. See https://pmg.proxmox.com/pmg-docs/api-viewer/ for the upstream documentation.

    The version of the OpenAPI document: 9.x
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from clientapi_pmg.models.pmg_boolean import PmgBoolean
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class ConfigClamavUpdateClamavSectionRequest(BaseModel):
    """
    ConfigClamavUpdateClamavSectionRequest
    """ # noqa: E501

    archiveblockencrypted: Optional[PmgBoolean] = Field(default=None, description="Whether to mark encrypted archives and documents as heuristic virus match. A match does not necessarily result in an immediate block, it just raises the Spam Score by 'clamav_heuristic_score'.")

    archivemaxfiles: Optional[Annotated[int, Field(strict=True, ge=0)]] = Field(default=1000, description="Number of files to be scanned within an archive, a document, or any other kind of container. Warning: disabling this limit or setting it too high may result in severe damage to the system.")

    archivemaxrec: Optional[Annotated[int, Field(strict=True, ge=1)]] = Field(default=5, description="Nested archives are scanned recursively, e.g. if a ZIP archive contains a TAR  file,  all files within it will also be scanned. This options specifies how deeply the process should be continued. Warning: setting this limit too high may result in severe damage to the system.")

    archivemaxsize: Optional[Annotated[int, Field(strict=True, ge=1000000)]] = Field(default=25000000, description="Files larger than this limit (in bytes) won't be scanned.")

    dbmirror: Optional[StrictStr] = Field(default='database.clamav.net', description="ClamAV database mirror server.")

    delete: Optional[Annotated[str, Field(strict=True, max_length=4096)]] = Field(default=None, description="A list of settings you want to delete.")

    digest: Optional[Annotated[str, Field(strict=True, max_length=64)]] = Field(default=None, description="Prevent changes if current configuration file has a different digest. This can be used to prevent concurrent modifications.")

    maxcccount: Optional[Annotated[int, Field(strict=True, ge=0)]] = Field(default=0, description="This option sets the lowest number of Credit Card or Social Security numbers found in a file to generate a detect.")

    maxscansize: Optional[Annotated[int, Field(strict=True, ge=1000000)]] = Field(default=100000000, description="Sets the maximum amount of data (in bytes) to be scanned for each input file.")

    scriptedupdates: Optional[PmgBoolean] = Field(default=None, description="Enables ScriptedUpdates (incremental download of signatures)")

    __properties: ClassVar[List[str]] = ["archiveblockencrypted", "archivemaxfiles", "archivemaxrec", "archivemaxsize", "dbmirror", "delete", "digest", "maxcccount", "maxscansize", "scriptedupdates"]













    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ConfigClamavUpdateClamavSectionRequest from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            # `exclude_unset` keeps schema defaults out of the wire payload
            # when the user constructed the model directly (e.g.
            # `Req(vmid=100)` would otherwise pull in
            # `cores=1, cpulimit=0, …` from the spec defaults and PVE
            # rejects the request with 400 because it never set those).
            # `exclude_none` keeps None values out of the wire payload —
            # both for direct construction (None means "unset") and for
            # the from_dict path (where unspecified obj keys become
            # `obj.get("k") == None` but show up in `model_fields_set`).
            exclude_unset=True,
            exclude_none=True,
        )
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ConfigClamavUpdateClamavSectionRequest from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "archiveblockencrypted": obj.get("archiveblockencrypted"),
            "archivemaxfiles": obj.get("archivemaxfiles") if obj.get("archivemaxfiles") is not None else 1000,
            "archivemaxrec": obj.get("archivemaxrec") if obj.get("archivemaxrec") is not None else 5,
            "archivemaxsize": obj.get("archivemaxsize") if obj.get("archivemaxsize") is not None else 25000000,
            "dbmirror": obj.get("dbmirror") if obj.get("dbmirror") is not None else 'database.clamav.net',
            "delete": obj.get("delete"),
            "digest": obj.get("digest"),
            "maxcccount": obj.get("maxcccount") if obj.get("maxcccount") is not None else 0,
            "maxscansize": obj.get("maxscansize") if obj.get("maxscansize") is not None else 100000000,
            "scriptedupdates": obj.get("scriptedupdates")
        })
        return _obj


