# coding: utf-8

"""
    Proxmox Mail Gateway API

    Generated from apidoc.js. NOT an official Proxmox specification. See https://pmg.proxmox.com/pmg-docs/api-viewer/ for the upstream documentation.

    The version of the OpenAPI document: 9.x
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from clientapi_pmg.models.pmg_boolean import PmgBoolean
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class ConfigRuledbCreateRuleRequest(BaseModel):
    """
    ConfigRuledbCreateRuleRequest
    """ # noqa: E501

    active: Optional[PmgBoolean] = Field(default=None, description="Flag to activate rule.")

    direction: Optional[Annotated[int, Field(le=2, strict=True, ge=0)]] = Field(default=None, description="Rule direction. Value `0` matches incoming mails, value `1` matches outgoing mails, and value `2` matches both directions.")

    from_and: Optional[PmgBoolean] = Field(default=None, description="Flag to 'and' combine FROM group matches.", alias="from-and")

    from_invert: Optional[PmgBoolean] = Field(default=None, description="Flag to invert FROM group matches.", alias="from-invert")

    name: StrictStr = Field(description="Rule name")

    priority: Annotated[int, Field(le=100, strict=True, ge=0)] = Field(description="Rule priority.")

    to_and: Optional[PmgBoolean] = Field(default=None, description="Flag to 'and' combine TO group matches.", alias="to-and")

    to_invert: Optional[PmgBoolean] = Field(default=None, description="Flag to invert TO group matches.", alias="to-invert")

    what_and: Optional[PmgBoolean] = Field(default=None, description="Flag to 'and' combine WHAT group matches.", alias="what-and")

    what_invert: Optional[PmgBoolean] = Field(default=None, description="Flag to invert WHAT group matches.", alias="what-invert")

    when_and: Optional[PmgBoolean] = Field(default=None, description="Flag to 'and' combine WHEN group matches.", alias="when-and")

    when_invert: Optional[PmgBoolean] = Field(default=None, description="Flag to invert WHEN group matches.", alias="when-invert")

    __properties: ClassVar[List[str]] = ["active", "direction", "from-and", "from-invert", "name", "priority", "to-and", "to-invert", "what-and", "what-invert", "when-and", "when-invert"]















    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ConfigRuledbCreateRuleRequest from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            # `exclude_unset` keeps schema defaults out of the wire payload
            # when the user constructed the model directly (e.g.
            # `Req(vmid=100)` would otherwise pull in
            # `cores=1, cpulimit=0, …` from the spec defaults and PVE
            # rejects the request with 400 because it never set those).
            # `exclude_none` keeps None values out of the wire payload —
            # both for direct construction (None means "unset") and for
            # the from_dict path (where unspecified obj keys become
            # `obj.get("k") == None` but show up in `model_fields_set`).
            exclude_unset=True,
            exclude_none=True,
        )
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ConfigRuledbCreateRuleRequest from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "active": obj.get("active"),
            "direction": obj.get("direction"),
            "from-and": obj.get("from-and"),
            "from-invert": obj.get("from-invert"),
            "name": obj.get("name"),
            "priority": obj.get("priority"),
            "to-and": obj.get("to-and"),
            "to-invert": obj.get("to-invert"),
            "what-and": obj.get("what-and"),
            "what-invert": obj.get("what-invert"),
            "when-and": obj.get("when-and"),
            "when-invert": obj.get("when-invert")
        })
        return _obj


