# coding: utf-8

"""
    Proxmox Mail Gateway API

    Generated from apidoc.js. NOT an official Proxmox specification. See https://pmg.proxmox.com/pmg-docs/api-viewer/ for the upstream documentation.

    The version of the OpenAPI document: 9.x
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class NodesPbsListTimerResponseData(BaseModel):
    """
    NodesPbsListTimerResponseData
    """ # noqa: E501

    delay: Optional[Annotated[str, Field(strict=True)]] = Field(default='5min', description="Randomized delay to add to the starttime (RandomizedDelaySec setting of the systemd.timer)")

    next_run: Optional[StrictStr] = Field(default=None, description="The date time of the next run, in server locale.", alias="next-run")

    remote: Optional[Annotated[str, Field(strict=True)]] = Field(default=None, description="Proxmox Backup Server remote ID.")

    schedule: Optional[Annotated[str, Field(strict=True)]] = Field(default='daily', description="Schedule for the backup (OnCalendar setting of the systemd.timer)")

    unitfile: Optional[StrictStr] = Field(default=None, description="unit file for the systemd.timer unit")

    __properties: ClassVar[List[str]] = ["delay", "next-run", "remote", "schedule", "unitfile"]


    @field_validator('delay')
    def delay_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"[0-9a-zA-Z. ]+", value):
            raise ValueError(r"must validate the regular expression /[0-9a-zA-Z. ]+/")
        return value



    @field_validator('remote')
    def remote_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"^[A-Za-z][A-Za-z0-9_]*$", value):
            raise ValueError(r"must validate the regular expression /^[A-Za-z][A-Za-z0-9_]*$/")
        return value


    @field_validator('schedule')
    def schedule_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"[0-9a-zA-Z*.:,\-\/ ]+", value):
            raise ValueError(r"must validate the regular expression /[0-9a-zA-Z*.:,\-\/ ]+/")
        return value




    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of NodesPbsListTimerResponseData from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            # `exclude_unset` keeps schema defaults out of the wire payload
            # when the user constructed the model directly (e.g.
            # `Req(vmid=100)` would otherwise pull in
            # `cores=1, cpulimit=0, …` from the spec defaults and PVE
            # rejects the request with 400 because it never set those).
            # `exclude_none` keeps None values out of the wire payload —
            # both for direct construction (None means "unset") and for
            # the from_dict path (where unspecified obj keys become
            # `obj.get("k") == None` but show up in `model_fields_set`).
            exclude_unset=True,
            exclude_none=True,
        )
        
        
        
        
        
        
        
        
        
        
        
        
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of NodesPbsListTimerResponseData from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "delay": obj.get("delay") if obj.get("delay") is not None else '5min',
            "next-run": obj.get("next-run"),
            "remote": obj.get("remote"),
            "schedule": obj.get("schedule") if obj.get("schedule") is not None else 'daily',
            "unitfile": obj.get("unitfile")
        })
        return _obj


